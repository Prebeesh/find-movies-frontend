service:
  url:
    movieNameService: ${PORT:http://MovieNameService/movies/}
server:
  port: ${PORT:8081}
spring:
  application:
    name: FindMovies_FrontEnd
  mvc:
    view:
      prefix: /webapp/views/
      suffix: .html
